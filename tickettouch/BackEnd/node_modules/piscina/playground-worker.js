// worker.js
const vm = require('vm')

module.exports = ({ type }) => {
  if (type === 'vm') {
    const script = new vm.Script('throw new Error("test error from vm")')
    script.runInNewContext({})
    console.log('error')
  } else {
    throw new Error('test error')
  }
}
